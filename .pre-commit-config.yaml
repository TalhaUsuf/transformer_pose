default_language_version:
    python: python3.10
repos:
# Automated hooks
-   repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.3.0
    hooks:
    -   id: check-added-large-files
        args: [
          "--maxkb=10000", # 10MB limit
          "--enforce-all"
          ] # prevent files large than 50 mb
    -   id: check-ast
    -   id: check-docstring-first
        # exclude a file
        exclude: src/data_upload/data_upload_service.py
    -   id: trailing-whitespace
    -   id: end-of-file-fixer
    -   id: check-yaml
    -   id: detect-private-key

    -   id: check-json
    -   id: check-merge-conflict
        args: [
          "--assume-in-merge"
          ]
-   repo: https://github.com/psf/black
    rev: 22.8.0
    hooks:
    # -   id: black-jupyter
    #     args: [--line-length=120]
    -   id: black
        language_version: python3.8
        args: [--line-length=300]
-   repo: https://github.com/PyCQA/isort
    rev: 5.10.1
    hooks:
    -   id: isort
        args: ["--profile", "black", "--filter-files"]
-   repo: https://github.com/PyCQA/autoflake
    rev: v1.5.3
    hooks:
      - id: autoflake
        # name: Remove unused variables and imports
        # entry: bash -c 'autoflake "$@"; git add -u' --
        language: python
        args:
          [
            "--in-place",
            "--remove-all-unused-imports",
            "--remove-unused-variables",
            "--expand-star-imports",
            "--ignore-init-module-imports",
          ]
        files: \.py$
# Restrictive hooks
-   repo: https://github.com/econchick/interrogate
    rev: 1.5.0
    hooks:
      - id: interrogate
        args: [-vv, -i, -I, -m, -M, -P, -n, -C,
            --exclude=tests,
            --exclude=google*,
            --exclude=repots,
            --exclude=docker,
            --exclude=setup.py,
            --fail-under=100]
        pass_filenames: false
-   repo: https://github.com/pre-commit/mirrors-autopep8
    rev: 'v1.7.0'  # Use the sha / tag you want to point at
    hooks:
    -   id: autopep8
# -   repo: https://github.com/pre-commit/mirrors-autopep8
#     rev: v1.4.4
#     hooks:
    # -   id: autopep8



# -   repo: https://github.com/PyCQA/bandit
#     rev: 1.7.4
#     hooks:
#     -   id: bandit
#         args: [-r, -ll,-ii]
# - repo: https://github.com/nbQA-dev/nbQA
#   rev: 1.3.1
#   hooks:
#     - id: nbqa-flake8
#       args: ["--max-line-length=120", "--ignore=E203,W503,E402"]
#     - id: nbqa-black
#       args: [--line-length=120]
# -   repo: https://github.com/PyCQA/flake8
#     rev: 4.0.1
#     hooks:
#     -   id: flake8
#         types: [file, python]
#         args: ["--max-line-length=150", "--ignore=E203,W503"]
# -   repo: https://github.com/pre-commit/mirrors-mypy
#     rev: v0.961
#     hooks:
#     -   id: mypy
#         args: [--no-strict-optional, --ignore-missing-imports]
#         additional_dependencies: [tokenize-rt==3.2.0]
